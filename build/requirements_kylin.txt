# 麒麟系统依赖包
# 适用于银河麒麟、中标麒麟等发行版

# 核心依赖 - 所有架构通用
numpy>=1.19.2
pandas>=1.2.0
scikit-learn>=0.24.0
scipy>=1.6.0
psutil>=5.8.0
joblib>=1.0.0
pyyaml>=6.0

# 机器学习依赖
# 注意：龙芯架构可能需要特殊版本的xgboost
xgboost>=1.5.0; platform_machine != "loongarch64"

# 数据处理和可视化
matplotlib>=3.3.0; platform_machine != "loongarch64"
seaborn>=0.11.0; platform_machine != "loongarch64"
imbalanced-learn>=0.8.0; platform_machine != "loongarch64"

# 输入监控依赖
# 注意：麒麟系统可能需要特殊配置
pynput>=1.7.6
keyboard>=0.13.5; platform_machine != "loongarch64"

# Linux系统特定依赖
python-xlib>=0.31; platform_system == "Linux"
python-evdev>=1.6.1; platform_system == "Linux" and platform_machine != "loongarch64"

# 打包依赖
pyinstaller>=5.0.0
setuptools>=42.0.0
wheel>=0.36.0

# 龙芯架构特殊依赖
# 如果是龙芯架构，可能需要从特殊源安装某些包
# 建议使用: pip install --find-links http://pypi.loongnix.cn/loongarch64/ package_name
